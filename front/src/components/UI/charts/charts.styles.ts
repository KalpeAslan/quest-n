import styled from "@emotion/styled";

export const ChartsStylesFix = styled.div`
  display: flex;
  min-width: 10px;
`;

export const ChartsStylesWrapper = styled.section`
  position: relative;
  flex: 1;
  width: 0;
`;

export const ChartsStylesHeader = styled.header`
  display: flex;
  justify-content: space-between;
`;

export const ChartsStylesTooltip = styled.p`
  padding: 0.4rem 0.5rem;
  color: #fafafa;
  background: rgb(255 255 255 / 15%);
  border-radius: 4px;
`;
