import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const Wrapper = styled(Box)`
  width: 100%;
  max-width: 680px;

  .btnBack {
    margin-right: 32px;
    max-width: 120px;
  }

  .btnSave {
    max-width: 328px;
  }
`;
